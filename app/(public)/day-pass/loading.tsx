import { PublicPageSkeleton } from '../_components/public-page-skeleton'

export default function Loading() {
  return <PublicPageSkeleton eyebrow="Day passes" title="Spend the day with us" body="booking" />
}
