import { PublicPageSkeleton } from '../_components/public-page-skeleton'

export default function Loading() {
  return (
    <PublicPageSkeleton
      eyebrow="Short stays"
      title="Check what is free, and book it"
      body="booking"
    />
  )
}
